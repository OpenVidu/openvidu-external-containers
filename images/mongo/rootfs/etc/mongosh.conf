# Global mongosh settings: mongosh, which the initialization scripts run, never contacts
# MongoDB, so there is no snippet index download, update check or telemetry.
mongosh:
  forceDisableTelemetry: true
  snippetIndexSourceURLs: ""
  updateURL: ""
