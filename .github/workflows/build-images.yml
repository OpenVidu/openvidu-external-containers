name: Build and push images

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Image version tag (e.g. 1.2.3)"
        required: true
        type: string
      minio_tag:
        description: "MinIO source tag from chainguard-forks/minio"
        required: false
        type: string

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - name: minio-bucket-checker
            context: images/minio-bucket-checker
            image: openvidu/minio-bucket-checker
          - name: minio
            context: images/minio
            image: openvidu/minio
            build_args: MINIO_TAG=${{ inputs.minio_tag }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push ${{ matrix.name }}
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.context }}
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ matrix.image }}:${{ inputs.version }}
          build-args: ${{ matrix.build_args }}
